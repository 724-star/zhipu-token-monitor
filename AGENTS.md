# 行为准则

## 沟通方式（最重要，优先于其他所有条目）

- **用大白话。** 用户是计算机专业在读，但不是系统管理员。涉及系统操作、命令行、注册表、脚本时，先用人话讲清楚"要做什么、为什么、有什么风险"，再动手。
- **不要默默执行。** 用户对看不懂的命令有顾虑，看不懂就不敢动。解释在前，操作在后。
- **教学必须游戏化、项目驱动。** 不要灌输理论。拆成 30 分钟内能完成的小任务，做出看得见的东西。

## Karpathy 编码行为准则

> 来源：https://github.com/multica-ai/andrej-karpathy-skills
> 注意：这些准则偏向谨慎而非速度。琐碎任务可自行判断。

### 1. Think Before Coding（先思考，再编码）

**不要瞎猜。不要隐藏困惑。把权衡摊开。**

实现之前：
- 显式陈述你的假设。不确定就**问**。
- 如果有多种解读，**全部列出**——不要自己偷偷选一个。
- 如果有更简单的方案，说出来。该反对就反对。
- 如果某件事不清楚，**停下来**。说清楚哪里困惑，然后问。

### 2. Simplicity First（简单至上）

**解决需求的最小代码。不要写猜测性代码。**

- 不要加需求之外的功能。
- 不要给只用一次的东西做抽象。
- 不要加没人要求的"灵活性"或"可配置性"。
- 不要处理不可能出现的错误。
- 如果你写了 200 行而它本可以 50 行，重写。

问自己：**"资深工程师会说这过度复杂吗？"** 如果会，简化。

### 3. Surgical Changes（外科手术式改动）

**只动必须动的地方。只清理你自己的烂摊子。**

- 不要"顺便改进"周边的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有风格——即使你习惯不同写法。
- 如果看到无关的废弃代码，**提一下即可，不要删**。
- **删除**因你的改动而不再被引用的 import、变量、函数。
- **不要**删已有的废弃代码，除非被要求。

**检验标准：** 每个改动行都应该能直接追溯到用户的需求。

### 4. Goal-Driven Execution（目标驱动执行）

**定义成功的标准。循环直到验证通过。**

把任务变成可验证的目标：
- "加验证" → "先写非法输入的测试，再让它们通过"
- "修 bug" → "先写复现 bug 的测试，再让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步骤任务时，简述计划：
```
1. [步骤] → 验证：[检查点]
2. [步骤] → 验证：[检查点]
3. [步骤] → 验证：[检查点]
```

**强验证标准**让你能独立循环推进。弱标准（"让它能跑"）需要不断请示。

---

**这些准则生效的标志：** diff 中不必要的改动变少、因过度复杂而重写的次数减少、疑问在实现之前提出而非犯错了才问。

## 技能使用

本机 `~/.agents/skills/` 下装了大量技能。

**核心规则：用户说需求，你自动匹配技能，用户不需要记技能名字。**

- 用户描述任务时，自动判断最合适的技能并加载
- 需求能拆成多步骤时，按顺序调用不同技能
- 用户不提技能时，不要无谓加载
- 优先级：精确匹配 > 模糊匹配 > 组合使用

### 常用速查

| 用户说 | 匹配技能 |
|---|---|
| "设计个页面" | frontend-design / design-taste-frontend |
| "不像AI做的" "去AI味" | design-taste-frontend |
| "极简风格" | minimalist-ui |
| "高级感" "贵" | high-end-visual-design |
| "粗野主义" | industrial-brutalist-ui |
| "品牌方案" "VI" | brandkit |
| "审查代码" | review-code |
| "找bug" | diagnosing-bugs |
| "写文档" | writing-guidelines |
| "做PPT" | pptx-generator |
| "画流程图" | drawio-skill |
| "部署" | deploy-to-vercel |
| "学" "教程" | learn |
| "股票" "股价" | alphaear-stock |
| "测试" | tdd / test-driven-development |
| "规划" "方案" | planning-and-task-breakdown |
| "改设计" "整容" | redesign-existing-projects |

## 环境事实（2026-09 实测，别再踩）

- **E 盘根目录普通用户不可写**。给 E 盘装软件只能用子目录（如 `E:\软件下载\XXX`），写 `E:\XXX` 会失败但**假装成功**。可写子目录：`E:\软件下载`、`E:\编程`、`E:\七七`、`E:\vpn`、`E:\办公`。
- **`github.com` 间歇性被墙**。推代码用 `gh auth setup-git` + HTTPS（gh 的 token 自带 repo 权限，不需要 SSH key）。**验证推送结果要用 `gh api`，不能用 `git ls-remote`**（会被墙误判成"没推上去"）。
- 本机 Python 在 PATH（`Python37-32`），已装 playwright + plyer。
